import QtQuick
import Dsqt.TouchEngine

// Convenience alias for DsTouchEngineView. This preserves the former type
// name, but the rewritten session-based API intentionally replaces the old
// instanceId/autoUpdate contract.
DsTouchEngineView {
}
